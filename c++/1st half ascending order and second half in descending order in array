#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int n,i,a[20];
	cin>>n;
	for(i=0;i<n;i++)
	{
	    cin>>a[i];
	}
	int f=0;
	int l=n;
	int mid=(f+l)/2;
	sort(a,a+mid-1);
	sort(a+(mid),a+n,greater<int>());
	for(i=0;i<n;i++)
	{
	    cout<<a[i]<<" ";
	}
	return 0;
}
