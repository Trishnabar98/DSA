#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int n,i,a[10];
	cin>>n;
	for(i=0;i<n;i++)
	{
	    cin>>a[i];
	}
	map<int,int> mp;
	for(i=0;i<n;i++)
	{
	    ++mp[a[i]];
	}
	for(auto it=mp.begin();it!=mp.end();it++)
	{
	    cout<<it->first<<" ";
	}
	
	
	return 0;
}
