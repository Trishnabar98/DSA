/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     struct ListNode *next;
 * };
 */


int getDecimalValue(struct ListNode* head){
    int r=0;
    while(head)
    {
        r=r*2+head->val;
        head=head->next;
      
    }
    return r;

}
